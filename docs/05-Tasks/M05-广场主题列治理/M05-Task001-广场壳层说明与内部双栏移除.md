# M05-Task001 - 广场壳层说明与内部双栏移除

**What to build:** `/about` 不再展示壳层说明卡；广场主题列以完整单列内容流呈现，不在主题列内部再划分右栏。全局导航列与辅助列保持不变。

**Blocked by:** None - can start immediately.

**Status:** completed

- [x] `/about` 不展示 `Campus Square` 壳层说明卡。
- [x] 桌面端主题列不再存在页面内部双栏。
- [x] 主题列内容宽度填满全局中间列。
