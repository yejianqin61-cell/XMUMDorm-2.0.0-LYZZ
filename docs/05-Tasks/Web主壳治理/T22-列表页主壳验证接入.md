# T22-列表页主壳验证接入

## 1. 任务目标

以 `MerchantList` 为代表页，验证 `ListPageLayout` 在新主壳中的落位是否成立。

## 2. 影响范围

- `frontend/src/pages/MerchantList.jsx`
- 主站壳接入相关文件

## 3. 不可触碰边界

- 不批量迁移其他列表页
- 不在本任务中重构筛选逻辑

## 4. 实施步骤

1. 让 `MerchantList` 接入新主壳。
2. 验证列表模板与主内容容器宽度关系。
3. 验证主区与右侧辅助区协作关系。

## 5. 验收方式

- `MerchantList` 在新主壳中正常显示
- 列表页结构节奏稳定
- `npm run build:web` 通过

## 6. 推荐提交信息

`refactor(web-shell): validate list page inside desktop shell`
