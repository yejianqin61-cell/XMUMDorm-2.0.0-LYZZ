# 前端上线前待开发备忘录

> 来源：用户提供的上线前自检清单。本文用于记录待治理事项，不等同于已拆分的开发 Task 或 GitHub Issue。
>
> 状态说明：`Archived` 表示本轮视为已完成归档；`Completed` 表示已完成开发并保留记录；`Backlog` 表示待安排；`Unspecified` 表示原始清单没有提供具体内容。

## 当前摘要

- `Archived`：3 项
- `Completed`：5 项
- `Phase3`：3 项
- `Backlog`：3 项
- `Unspecified`：1 项
- 本备忘录不包含已废弃的 `mobile/` RN 前端。

## 事项清单

| 编号 | 状态 | 事项 | 目标与边界 |
| --- | --- | --- | --- |
| 1 | Archived | 帖子详情页原生体验 | 详情页横向固定，禁止页面级左右位移、相邻页面暴露和浏览器式网页感；保留图片轮播、图片预览、纵向阅读、评论和系统返回。 |
| 2 | Archived | 公告体验优化 | 覆盖公告发布、通知投递、未读弹窗、信箱预览、公告详情和已读操作的完整链路；公告标题、正文、发布者和时间应一致展示。 |
| 3 | Archived | 特别鸣谢人称准确 | 已将非教授身份的称谓调整为“老师/Instructor”，将明确非教授身份的称谓调整为“先生/Mr.”，并同步 App 与 Web 端。 |
| 4 | Backlog | 个人空间整体体验优化 | 重构个人空间的信息层级、入口排布、留白、卡片密度和交互反馈，消除当前页面堆叠、粗糙和不成体系的体验。 |
| 5 | Completed | 广场功能入口排布优化 | App 已改为完整静态四宫格，轮播图置于入口上方，入口图标改为 48px 黑色线描镂空图标；必要功能入口和业务路径均保留。 |
| 6 | Completed | 轮播图管理员界面优化 | 已补齐未上线轮播图的可见性和状态辨识，管理员可继续查看、编辑和管理全部轮播图。 |
| 7 | Completed | Todo 页面体验优化 | Todo 已改为页内创建、乐观完成状态和 X 风格分隔线列表；筛选使用下划线选中态，新建入口保留居中圆角按钮。 |
| 8 | Completed | 关于我们内容收束 | “免责声明”和“联系我们”已收束到“关于我们”信息架构下。 |
| 9 | Completed | 设置入口收束 | “编辑资料”“退出登录”“注销账号”已统一收束到设置页面，并保留危险操作确认和反馈。 |
| 10 | Phase3 | 轮播图设置导向页面 | 新增轮播图设置导向页，作为相关管理入口的统一导航；明确权限、当前状态和下一步操作。 |
| 11 | Backlog | 通知系统全链路测试 | 覆盖发布、收件人筛选、未读查询、弹窗、信箱、详情跳转、单条已读、批量已读、刷新、重复登录和历史数据兼容。 |
| 12 | Backlog | 通知系统 UI 优化 | 统一通知弹窗、信箱列表、分类 Tab、未读标识、空状态、加载状态和错误反馈，避免乱码、中英混搭和无关聚合文案。 |
| 13 | Backlog | 公告弹窗正文展示 | 代码层已有正文渲染和详情入口，但仍需真实确认管理员发布后，普通用户在 Web/APK 的弹窗中确实看到正文；同时验证长文本、换行、空正文、历史通知和失败恢复。 |
| 14 | Phase3 | 移除树洞瀑布流算法推荐话术 | 删除“继续沿着你的兴趣看”“和你的校园身份更相关”等暗示算法推荐的文案；在尚未实现推荐算法前，不向用户暗示个性化排序。 |
| 15 | Phase3 | 底部 Tab 栏体验优化 | 优化底部 Tab 的视觉层级、触控反馈和切换动效，目标是接近 iOS/Apple 风格的连续、克制和顺滑体验；不得影响可访问性和路由状态。 |
| 16 | Unspecified | 原始清单未提供具体内容 | 暂不创建开发任务，等待补充明确的问题描述、页面范围和验收目标。 |

## 当前待开发清单

1. Phase3：轮播图设置导向页面（10）。
2. Phase3：移除树洞瀑布流中的算法推荐话术（14）。
3. Phase3：底部 Tab 栏视觉与切换动效优化（15）。
4. 个人空间整体体验优化（4）。
5. 通知系统全链路测试和通知 UI 治理（11、12）。
6. 公告弹窗正文的真实 Web/APK 回归确认（13）。

## 建议治理顺序

1. Phase3 先处理轮播图设置导向（10），明确管理员入口和权限边界。
2. 随后清理树洞算法推荐话术（14），再优化底部 Tab（15）。
3. Phase3 末尾进行 App/Web 双端路由、语言模式、Android 返回键和安全区域回归。
4. Phase3 之外继续安排个人空间（4）和通知系统治理（11、12）；公告正文（13）单独做真实链路确认。

## 归档说明

- 事项 1、2 已按当前 Phase 结果标记为 `Archived`，不应重复创建同范围 Task；后续若出现新问题，应创建针对具体回归缺陷的独立事项。
- 事项 3 已完成 App 与 Web 端同步，并将在对应 GitHub Issue 创建后立即关闭。
- 事项 11、12、13 虽与已归档的公告工作相关，但本备忘录中的范围是后续全链路测试和整体 UI 治理，不能仅凭已有局部修复视为完成。
- 事项 5、6、7、8、9 已完成本轮 Phase2 范围，保留为 `Completed` 供上线回归时核对；后续若出现缺陷，应按具体回归问题单独记录，不重复打开原范围。
- 事项 10、14、15 已纳入 Phase3；第 16 项按当前决策继续保持 `Unspecified`，不进入 Phase3。

## 第 2 项与第 13 项的边界

### 第 2 项：公告体验优化（已归档）

第 2 项是公告能力的整体治理目标，范围包括管理员发布、通知投递、未读弹窗、信箱预览、公告详情、单条/批量已读，以及标题、正文、发布者和时间的数据语义一致性。它回答的是“公告链路是否存在并能完整走通”。

### 第 13 项：公告弹窗正文展示（待确认）

第 13 项是对第 2 项中“未读弹窗展示”这一节点的专项验收，关注的是“用户实际打开弹窗时，正文是否真的显示”。它不重新建设公告链路，而是确认运行时数据映射、历史通知兼容和视口表现。

第 13 项仍需确认：

- 管理员发布一条含正文的公告后，普通用户在 Web 和 APK 都能看到正文，而不是只看到标题或空白。
- 长正文、换行、特殊字符和超长文本在弹窗内部可读，不撑破屏幕。
- 历史通知缺少正文时显示稳定的回退状态，不出现乱码、`undefined` 或 raw JSON。
- 弹窗的“查看详情”入口能进入正确公告帖子；单条已读和关闭行为不丢失正文状态。
- 接口失败、重复登录、刷新和多条未读公告并存时，正文展示和未读状态仍一致。
