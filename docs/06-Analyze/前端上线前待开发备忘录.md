# 前端上线前待开发备忘录

> 来源：用户提供的上线前自检清单。本文用于记录待治理事项，不等同于已拆分的开发 Task 或 GitHub Issue。
>
> 状态说明：`Archived` 表示本轮视为已完成归档；`Backlog` 表示待安排；`Unspecified` 表示原始清单没有提供具体内容。

## 当前摘要

- `Archived`：3 项
- `Backlog`：11 项
- `Unspecified`：1 项
- 本备忘录不包含已废弃的 `mobile/` RN 前端。

## 事项清单

| 编号 | 状态 | 事项 | 目标与边界 |
| --- | --- | --- | --- |
| 1 | Archived | 帖子详情页原生体验 | 详情页横向固定，禁止页面级左右位移、相邻页面暴露和浏览器式网页感；保留图片轮播、图片预览、纵向阅读、评论和系统返回。 |
| 2 | Archived | 公告体验优化 | 覆盖公告发布、通知投递、未读弹窗、信箱预览、公告详情和已读操作的完整链路；公告标题、正文、发布者和时间应一致展示。 |
| 3 | Archived | 特别鸣谢人称准确 | 已将非教授身份的称谓调整为“老师/Instructor”，将明确非教授身份的称谓调整为“先生/Mr.”，并同步 App 与 Web 端。 |
| 4 | Backlog | 个人空间整体体验优化 | 重构个人空间的信息层级、入口排布、留白、卡片密度和交互反馈，消除当前页面堆叠、粗糙和不成体系的体验。 |
| 5 | Backlog | 广场功能入口排布优化 | 优化“社团广场”“马校一站通”等入口 Tab/功能区的布局、顺序和视觉层级；保留必要功能入口，不改变其业务可达性。 |
| 6 | Backlog | 轮播图管理员界面优化 | 优化管理员轮播图列表、预览、排序、启停、编辑和删除等操作的布局与反馈，降低后台操作复杂度。 |
| 7 | Backlog | Todo 页面体验优化 | 优化 Todo 列表、完成状态反馈、空状态和错误状态；将创建 Todo 页面收束到创建按钮触发的界面内部，减少独立页面跳转。 |
| 8 | Backlog | 关于我们内容收束 | 将“免责声明”和“联系我们”收束到“关于我们”信息架构下，减少顶层入口和页面分散。 |
| 9 | Backlog | 设置入口收束 | 将“编辑资料”“退出登录”“注销账号”统一收束到设置页面，并明确危险操作确认和反馈。 |
| 10 | Backlog | 轮播图设置导向页面 | 新增轮播图设置导向页，作为相关管理入口的统一导航；明确权限、当前状态和下一步操作。 |
| 11 | Backlog | 通知系统全链路测试 | 覆盖发布、收件人筛选、未读查询、弹窗、信箱、详情跳转、单条已读、批量已读、刷新、重复登录和历史数据兼容。 |
| 12 | Backlog | 通知系统 UI 优化 | 统一通知弹窗、信箱列表、分类 Tab、未读标识、空状态、加载状态和错误反馈，避免乱码、中英混搭和无关聚合文案。 |
| 13 | Backlog | 公告弹窗正文展示 | 确保公告弹窗实际显示正文内容，并提供标题、正文、发布者、时间和公告详情入口；长文本不得撑破视口。 |
| 14 | Backlog | 移除树洞瀑布流算法推荐话术 | 删除“继续沿着你的兴趣看”“和你的校园身份更相关”等暗示算法推荐的文案；在尚未实现推荐算法前，不向用户暗示个性化排序。 |
| 15 | Backlog | 底部 Tab 栏体验优化 | 优化底部 Tab 的视觉层级、触控反馈和切换动效，目标是接近 iOS/Apple 风格的连续、克制和顺滑体验；不得影响可访问性和路由状态。 |
| 16 | Unspecified | 原始清单未提供具体内容 | 暂不创建开发任务，等待补充明确的问题描述、页面范围和验收目标。 |

## 建议治理顺序

1. 先完成通知系统的全链路测试（11），再处理通知 UI 与公告弹窗展示（12、13），避免只修视觉而遗漏数据链路。
2. 随后处理设置、关于我们和 Todo 的信息架构收束（7、8、9、10），减少入口分散。
3. 再进行个人空间、广场入口、管理员轮播图和底部 Tab 的整体 UI 优化（4、5、6、15）。
4. 最后处理文案与内容清理（3、14），并对全站中英文模式做一次回归。

## 归档说明

- 事项 1、2 已按当前 Phase 结果标记为 `Archived`，不应重复创建同范围 Task；后续若出现新问题，应创建针对具体回归缺陷的独立事项。
- 事项 3 已完成 App 与 Web 端同步，并将在对应 GitHub Issue 创建后立即关闭。
- 事项 11、12、13 虽与已归档的公告工作相关，但本备忘录中的范围是后续全链路测试和整体 UI 治理，不能仅凭已有局部修复视为完成。
