# 移动端 App 认证模块测试报告

**项目：** Dorm · XMUMDorm-2.0.0-LYZZ  
**测试对象：** `mobile/` 认证模块（AuthContext + LoginScreen + RegisterScreen）  
**测试日期：** 2026-06-01  
**测试框架：** Jest 30.4.2 + ts-jest + AsyncStorage Mock

---

## 一、测试环境

| 项目 | 配置 |
|------|------|
| 测试运行器 | Jest 30.4.2 |
| 测试环境 | Node（无 DOM/React Native 渲染） |
| Mock 策略 | `moduleNameMapper` 映射 AsyncStorage |
| 测试格式 | CommonJS（`.test.js`），避免 babel/TSX 兼容问题 |
| API Mock | `global.fetch` → `jest.fn()` |

---

## 二、测试覆盖范围

| 模块 | 测试用例数 | 覆盖状态 |
|------|-----------|----------|
| AuthContext 逻辑层 | 12 | ✅ |
| AuthContext API 格式 | 7 | ✅ |
| **合计** | **19** | **✅ 19/19 通过** |

---

## 三、测试用例清单

### 3.1 AuthContext — 逻辑层（12 用例）

| # | 用例 | 状态 |
|---|------|------|
| 1 | 登录 API 成功返回 token + username | ✅ |
| 2 | 登录失败返回错误消息 | ✅ |
| 3 | 网络错误不崩溃（try-catch 捕获） | ✅ |
| 4 | 有效 token → exp 在未来 → 未过期 | ✅ |
| 5 | 过期 token → exp 在过去 → 已过期 | ✅ |
| 6 | skipLogin → AsyncStorage 持久化标记 | ✅ |
| 7 | logout → multiRemove 清理三个 key（token/user/skipLogin） | ✅ |
| 8 | admin 角色 → isAdmin = true | ✅ |
| 9 | student 角色 → isAdmin = false | ✅ |
| 10 | displayName 优先 nickname | ✅ |
| 11 | 无 nickname 回退 username | ✅ |
| 12 | 都无回退"游客" | ✅ |

### 3.2 AuthContext — API 格式校验（7 用例）

| # | 用例 | 状态 |
|---|------|------|
| 13 | 邮箱登录 → 请求体含 `email` 字段 | ✅ |
| 14 | 学号登录 → 请求体含 `username` 字段 | ✅ |
| 15 | 非 @xmu.edu.my 邮箱 → 拒绝 | ✅ |
| 16 | @xmu.edu.my 邮箱 → 通过 | ✅ |
| 17 | 密码 < 6 位 → 拒绝 | ✅ |
| 18 | 密码 >= 6 位 → 通过 | ✅ |
| 19 | 注册请求完整字段（role/email/username/password/code） | ✅ |

---

## 四、测试结果

```
Test Suites: 1 passed, 1 total
Tests:       19 passed, 19 total
Snapshots:   0 total
Time:        0.333 s
```

**通过率：100%（19/19）**

---

## 五、未覆盖项（预期内）

| 未覆盖项 | 原因 |
|----------|------|
| LoginScreen UI 交互测试 | RN 渲染依赖 babel JSX 转换，当前 Jest 30 + RN 0.85 存在兼容问题，已改用纯逻辑测试 |
| RegisterScreen UI 测试 | 同上 |
| AuthContext React 渲染测试 | 同上 |
| 端到端登录流程 | 需要真机/模拟器环境 |

**说明：** 当前测试策略聚焦于核心逻辑层（token 管理、API 格式、表单校验、角色识别），UI 层测试待 RN Jest preset 稳定后补充。

---

## 六、后续测试计划

| 阶段 | 内容 | 预估 |
|------|------|------|
| 短期 | 补充 TreeholeScreen 数据获取逻辑测试 | 1h |
| 短期 | 补充 PostDetailModal 评论/点赞逻辑测试 | 1h |
| 中期 | 升级到 @react-native/jest-preset 稳定版，恢复 UI 测试 | 2h |
| 长期 | 引入 Detox / Maestro 端到端测试 | 4h |

---

**报告编写：** Claude Code (Claude Opus 4.8)  
**最后更新：** 2026-06-01
