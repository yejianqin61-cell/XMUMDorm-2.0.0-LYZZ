# 课程表模块 (Schedule) 测试报告

**日期**: 2026-06-01  
**模块**: 课程表 (Schedule / Timetable)  
**测试文件**: `mobile/__tests__/screens/ScheduleScreen.test.js`  
**测试框架**: Jest (Node 环境)  
**分支**: main

---

## 测试概览

| 指标 | 数值 |
|------|------|
| 总用例数 | 37 |
| 通过 | 37 ✅ |
| 失败 | 0 |
| 通过率 | 100% |
| 测试类型 | 单元测试 17 + 集成测试 7 + 提醒服务 13 |

---

## Part A — 单元测试（17 用例）

纯函数测试，无外部依赖。

### A1. getTodayDow — 日期映射（2 用例）

| # | 用例 | 状态 |
|---|------|------|
| A1 | 返回值范围 1-7 | ✅ |
| A2 | 非 0（JS 周日 → 7） | ✅ |

### A2. fmtTime — 时间格式化（3 用例）

| # | 用例 | 状态 |
|---|------|------|
| A3 | "14:00:00" → "14:00" | ✅ |
| A4 | 空字符串 / null / undefined → "--:--" | ✅ |
| A5 | 已截断字符串不变 "14:00" → "14:00" | ✅ |

### A3. addFreeSlots — 课间空档检测（6 用例）

| # | 用例 | 状态 |
|---|------|------|
| A6 | 3 小时间隔（180min ≥ 25）→ 插入 1 个 freeSlot | ✅ |
| A7 | 20 分钟间隔（< 25）→ 不插入 | ✅ |
| A8 | 边界：恰好 25 分钟 → 插入 | ✅ |
| A9 | 单节课 → 无空档 | ✅ |
| A10 | 空数组 [] → [] | ✅ |
| A11 | null → [] | ✅ |

**算法验证**: 空档检测阈值 25 分钟，边界包含（≥25），排序依赖时间字符串比较。

### A4. parseTime — 时间解析（3 用例）

| # | 用例 | 状态 |
|---|------|------|
| A12 | "14:00:00" → {hours:14, minutes:0} | ✅ |
| A13 | "09:30:00" → {hours:9, minutes:30} | ✅ |
| A14 | 空字符串 → {hours:0, minutes:0} | ✅ |

### A5. nextDayOfWeek — 下个星期计算（3 用例）

| # | 用例 | 状态 |
|---|------|------|
| A15 | 今天周一 → 下周一 = 7 天后 | ✅ |
| A16 | 今天周一 → 下周三 = 2 天后 | ✅ |
| A17 | 今天周一 → 下周日 = 6 天后（dow=7 映射为 JS 0） | ✅ |

**基准日期**: 2026-06-01（周一），验证跨周日映射。

---

## Part B — 集成测试（7 用例）

mock 后端 API，测试完整数据流。

### B1. Week API（3 用例）

| # | 用例 | 状态 |
|---|------|------|
| B1 | 完整 7 天课表 — days 包含 1-7 键 | ✅ |
| B2 | 课程字段完整性 — course_code / lecturer / credit / venue / week_start / week_end | ✅ |
| B3 | 空天返回 [] | ✅ |

### B2. Import API 完整流程（4 用例）

| # | 用例 | 状态 |
|---|------|------|
| B4 | Preview → 解析出 course + meeting，stats 正确 | ✅ |
| B5 | Preview → 解析含 3 个错误，errors 数组完整 | ✅ |
| B6 | Commit → 导入成功，返回 courseCount=3 meetingCount=8 | ✅ |
| B7 | **端到端**: Commit → Week GET 刷新，数据一致 | ✅ |

**端到端流程验证**: B7 模拟用户导入后刷新课表的完整链路。

---

## Part C — 提醒服务测试（13 用例）

### C1. calcReminderTime — 提醒时间计算（4 用例）

| # | 用例 | 状态 |
|---|------|------|
| C1 | 14:00 课前 30min → "13:30" | ✅ |
| C2 | 09:00 课前 30min → "08:30" | ✅ |
| C3 | 00:30 课前 30min → "00:00"（跨日边界） | ✅ |
| C4 | 自定义提前量 15min → "08:45" | ✅ |

### C2. dedupKey — 去重键（3 用例）

| # | 用例 | 状态 |
|---|------|------|
| C5 | 格式：`reminder_{meetingId}_{dow}_{startTime}` | ✅ |
| C6 | 不同 meetingId → 不同 key | ✅ |
| C7 | 不同 startTime → 不同 key | ✅ |

### C3. 视图数据（2 用例）

| # | 用例 | 状态 |
|---|------|------|
| C8 | 7 天标签：周一 ~ 周日 | ✅ |
| C9 | 缓存 key 格式：`dorm_schedule_cache_v1_w1` | ✅ |

### C4. 服务层 Mock 测试（4 用例）

| # | 用例 | 状态 |
|---|------|------|
| C10 | `requestReminderPermission()` 默认返回 true | ✅ |
| C11 | `scheduleAllReminders()` 返回 count=5 | ✅ |
| C12 | `cancelAllReminders()` 调用成功 | ✅ |
| C13 | `hasScheduledReminders()` 默认返回 false | ✅ |

**Mock 架构**: `expo-notifications` + `@react-native-async-storage/async-storage` 双重 mock，隔离原生依赖。

---

## 覆盖率分析

| 被测模块 | 函数/端点 | 测试数 | 覆盖维度 |
|---------|----------|--------|---------|
| `ScheduleScreen` | `getTodayDow` | 2 | 边界值 |
| `ScheduleScreen` | `fmtTime` | 3 | 正常/空/null |
| `ScheduleScreen` | `addFreeSlots` | 6 | 算法/边界/空/多课 |
| `scheduleReminder` | `parseTime` | 3 | 正常/边界/空 |
| `scheduleReminder` | `nextDayOfWeek` | 3 | 跨天/跨周/周日映射 |
| `scheduleReminder` | `calcReminderTime` | 4 | 正常/跨小时/跨日/自定义 |
| `scheduleReminder` | `dedupKey` | 3 | 格式/唯一性 |
| API | `GET /week` | 3 | 7天/字段完整/空天 |
| API | `POST /import/*` | 4 | preview/commit/错误/端到端 |
| 服务 | Mock 权限/调度/取消/状态 | 4 | 4 个导出函数 |

---

## 迁移文件清单

| 文件 | 说明 |
|------|------|
| `src/services/scheduleReminder.ts` | 通知调度服务：权限请求 + 批量排通知 + 去重 + 取消 |
| `src/screens/ScheduleScreen.tsx` | 课程表主页：7 天视图 + 今日高亮 + 空档检测 + 导入 Modal + 提醒开关 |
| `__tests__/screens/ScheduleScreen.test.js` | 37 测试用例（17 单元 + 7 集成 + 13 提醒服务） |

**API 端点**: 3 个（`GET /api/schedule/week` / `POST /api/schedule/import/preview` / `POST /api/schedule/import/commit`）  
**Mock 依赖**: `expo-notifications` / `@react-native-async-storage/async-storage` / `react-native` Platform

---

## 全量累计

```
认证:   19 ✅   树洞:  20 ✅   我的:  20 ✅
通知:   20 ✅   食堂:  20 ✅   广场:  20 ✅
社团:   20 ✅   二手:  20 ✅   跑腿:  20 ✅
等级:   20 ✅   一站通: 20 ✅   课程:  20 ✅
日记:   20 ✅   待办:  20 ✅   课表:  37 ✅
──────────────────────────────────────────
总计:  316 ✅   (100%)   15 suites
```
