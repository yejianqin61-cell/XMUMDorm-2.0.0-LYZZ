# Git 和 GitHub 完整教程

## 一、Git 和 GitHub 的关系

### 1. 什么是 Git？

**Git** 是一个**分布式版本控制系统**（Version Control System），用于：
- 跟踪文件的更改历史
- 保存代码的每个版本
- 多人协作开发
- 回退到任意历史版本

**类比**：
- Git = 你的本地"时光机" 📸
- 每次提交 = 拍一张"快照"
- 可以随时回到任何"快照"

### 2. 什么是 GitHub？

**GitHub** 是一个**代码托管平台**（基于 Git），用于：
- 将代码存储到云端
- 备份代码（防止丢失）
- 多人协作
- 展示项目（开源项目）

**类比**：
- GitHub = 云盘（如百度网盘）☁️
- 本地 Git = 你的电脑 💻
- 上传到 GitHub = 备份到云端

### 3. 它们的关系

```
┌─────────────────────────────────────────┐
│  你的电脑（本地）                        │
│  ┌─────────────────┐                   │
│  │  Git 仓库       │                   │
│  │  (本地版本控制)  │                   │
│  └─────────────────┘                   │
│         │                               │
│         │ git push (推送)               │
│         ▼                               │
└─────────────────────────────────────────┘
         │
         │ 网络
         ▼
┌─────────────────────────────────────────┐
│  GitHub（云端）                         │
│  ┌─────────────────┐                   │
│  │  Git 仓库       │                   │
│  │  (远程备份)     │                   │
│  └─────────────────┘                   │
└─────────────────────────────────────────┘
```

**简单理解**：
- **Git** = 工具（在你的电脑上）
- **GitHub** = 网站（在互联网上）
- Git 可以**不需要** GitHub 单独使用
- GitHub **必须**使用 Git 来工作

---

## 二、Git 基础指令详解

### 1. 初始化仓库

```bash
# 在当前文件夹创建 Git 仓库
git init
```

**作用**：创建一个 `.git` 隐藏文件夹，开始版本控制

**什么时候用**：第一次使用 Git 时

---

### 2. 查看状态

```bash
# 查看当前仓库状态
git status
```

**作用**：
- 显示哪些文件被修改了
- 显示哪些文件已暂存（准备提交）
- 显示哪些文件未跟踪

**输出示例**：
```
On branch master
Changes not staged for commit:
  modified:   routes/auth.js

Untracked files:
  new-file.js
```

---

### 3. 添加文件到暂存区

```bash
# 添加单个文件
git add 文件名

# 添加所有文件
git add .

# 添加特定类型的文件
git add *.js
```

**作用**：将文件添加到"暂存区"（准备提交）

**类比**：
- 工作区 = 你的桌子（正在编辑的文件）
- 暂存区 = 打包箱（准备提交的文件）
- 仓库 = 仓库（已保存的版本）

**工作流程**：
```
工作区 → git add → 暂存区 → git commit → 仓库
```

---

### 4. 提交更改

```bash
# 提交暂存区的所有文件
git commit -m "提交信息"
```

**作用**：将暂存区的文件保存到仓库，创建一个"快照"

**提交信息规范**：
```bash
# 好的提交信息
git commit -m "完成用户注册功能"
git commit -m "修复登录API路径问题"
git commit -m "添加帖子发布功能"

# 不好的提交信息（避免）
git commit -m "更新"
git commit -m "修复bug"
git commit -m "123"
```

**提交信息格式建议**：
```
类型: 简短描述

例如：
feat: 添加用户注册功能
fix: 修复登录验证问题
docs: 更新README文档
style: 格式化代码
refactor: 重构认证模块
```

---

### 5. 查看提交历史

```bash
# 查看简洁的提交历史
git log --oneline

# 查看详细的提交历史
git log

# 查看最近3次提交
git log -3

# 查看图形化的提交历史
git log --graph --oneline
```

**输出示例**：
```
fe1c809 (HEAD -> master) 初始化项目 - 认证模块已完成
```

**说明**：
- `fe1c809` = 提交ID（唯一标识）
- `HEAD` = 当前所在位置
- `master` = 分支名称

---

### 6. 查看文件差异

```bash
# 查看工作区和暂存区的差异
git diff

# 查看某个文件的差异
git diff 文件名

# 查看暂存区和仓库的差异
git diff --staged
```

**作用**：查看文件具体改了什么

---

### 7. 撤销更改

```bash
# 撤销工作区的更改（未暂存）
git checkout -- 文件名

# 撤销暂存区的更改（已暂存但未提交）
git reset HEAD 文件名

# 撤销最后一次提交（保留更改）
git reset --soft HEAD~1

# 撤销最后一次提交（丢弃更改）
git reset --hard HEAD~1
```

**⚠️ 警告**：`git reset --hard` 会永久删除更改，谨慎使用！

---

### 8. 恢复文件

```bash
# 从仓库恢复文件（如果文件丢失）
git checkout HEAD -- 文件名

# 从特定提交恢复文件
git checkout 提交ID -- 文件名
```

**作用**：如果文件丢失或误删，可以从 Git 历史中恢复

---

### 9. 创建分支

```bash
# 创建新分支
git branch 分支名

# 创建并切换到新分支
git checkout -b 分支名

# 查看所有分支
git branch

# 切换分支
git checkout 分支名

# 删除分支
git branch -d 分支名
```

**分支的作用**：
- 主分支（master/main）= 稳定版本
- 功能分支 = 开发新功能
- 互不干扰，可以并行开发

**示例**：
```bash
# 创建功能分支
git checkout -b feature/posts

# 在新分支上开发
# ... 编写代码 ...

# 提交更改
git add .
git commit -m "完成帖子功能"

# 切换回主分支
git checkout master

# 合并功能分支
git merge feature/posts
```

---

### 10. 合并分支

```bash
# 将指定分支合并到当前分支
git merge 分支名
```

**作用**：将功能分支的更改合并到主分支

---

## 三、GitHub 使用指南

### 第一步：创建 GitHub 账号

1. 访问 [GitHub官网](https://github.com)
2. 点击 "Sign up" 注册账号
3. 验证邮箱

---

### 第二步：创建 GitHub 仓库

1. 登录 GitHub
2. 点击右上角 "+" → "New repository"
3. 填写信息：
   - Repository name: `jack-campus-social`（仓库名）
   - Description: `校园社交网站`（描述，可选）
   - Public/Private: 选择公开或私有
   - **不要**勾选 "Initialize this repository with a README"（因为本地已有代码）
4. 点击 "Create repository"

---

### 第三步：连接本地仓库和 GitHub

#### 方法 1：使用 HTTPS（推荐新手）

```bash
# 1. 添加远程仓库地址
git remote add origin https://github.com/你的用户名/jack-campus-social.git

# 2. 查看远程仓库
git remote -v

# 3. 推送代码到 GitHub
git push -u origin master
```

**说明**：
- `origin` = 远程仓库的别名（可以自定义）
- `master` = 分支名（GitHub 现在默认是 `main`）

**如果 GitHub 仓库是 `main` 分支**：
```bash
# 重命名本地分支
git branch -M main

# 推送到 GitHub
git push -u origin main
```

#### 方法 2：使用 SSH（更安全，需要配置）

```bash
# 1. 生成 SSH 密钥
ssh-keygen -t ed25519 -C "your_email@example.com"

# 2. 复制公钥内容
cat ~/.ssh/id_ed25519.pub

# 3. 在 GitHub 添加 SSH 密钥
# Settings → SSH and GPG keys → New SSH key

# 4. 添加远程仓库
git remote add origin git@github.com:你的用户名/jack-campus-social.git

# 5. 推送代码
git push -u origin master
```

---

### 第四步：推送代码到 GitHub

```bash
# 第一次推送（建立连接）
git push -u origin master

# 之后的推送（直接推送）
git push
```

**完整流程**：
```bash
# 1. 查看状态
git status

# 2. 添加文件
git add .

# 3. 提交更改
git commit -m "完成XXX功能"

# 4. 推送到 GitHub
git push
```

---

### 第五步：从 GitHub 拉取代码

```bash
# 拉取远程更改并合并
git pull

# 或者分两步
git fetch        # 下载远程更改
git merge        # 合并到本地
```

**什么时候用**：
- 多人协作时，其他人推送了代码
- 在其他电脑上工作，需要同步最新代码

---

## 四、常用 Git 工作流程

### 日常开发流程

```bash
# 1. 开始工作前，拉取最新代码
git pull

# 2. 编写代码...

# 3. 查看更改
git status

# 4. 添加更改
git add .

# 5. 提交更改
git commit -m "完成XXX功能"

# 6. 推送到 GitHub
git push
```

---

### 如果文件丢失，如何恢复

```bash
# 1. 查看提交历史
git log --oneline

# 2. 找到丢失文件时的提交ID
# 例如：fe1c809

# 3. 恢复文件
git checkout fe1c809 -- 文件名

# 4. 提交恢复的文件
git add 文件名
git commit -m "恢复丢失的文件"
```

---

## 五、Git 指令速查表

### 基础操作

| 指令 | 作用 |
|------|------|
| `git init` | 初始化仓库 |
| `git status` | 查看状态 |
| `git add .` | 添加所有文件 |
| `git commit -m "信息"` | 提交更改 |
| `git log` | 查看提交历史 |
| `git diff` | 查看差异 |

### 远程操作

| 指令 | 作用 |
|------|------|
| `git remote add origin URL` | 添加远程仓库 |
| `git remote -v` | 查看远程仓库 |
| `git push` | 推送到远程 |
| `git pull` | 拉取远程更改 |
| `git clone URL` | 克隆远程仓库 |

### 分支操作

| 指令 | 作用 |
|------|------|
| `git branch` | 查看分支 |
| `git branch 分支名` | 创建分支 |
| `git checkout 分支名` | 切换分支 |
| `git merge 分支名` | 合并分支 |

### 撤销操作

| 指令 | 作用 |
|------|------|
| `git checkout -- 文件` | 撤销工作区更改 |
| `git reset HEAD 文件` | 撤销暂存区更改 |
| `git reset --hard HEAD~1` | 撤销最后一次提交 |

---

## 六、将当前项目推送到 GitHub

### 完整步骤

```bash
# 1. 确保在项目目录
cd C:\Users\叶健钦\Desktop\Jack

# 2. 查看当前状态
git status

# 3. 如果有未提交的更改，先提交
git add .
git commit -m "更新项目"

# 4. 在 GitHub 创建仓库后，添加远程地址
# （替换为你的 GitHub 用户名和仓库名）
git remote add origin https://github.com/你的用户名/jack-campus-social.git

# 5. 如果本地分支是 master，GitHub 是 main，需要重命名
git branch -M main

# 6. 推送到 GitHub
git push -u origin main
```

**如果遇到错误**：

1. **错误：remote origin already exists**
   ```bash
   # 删除现有远程仓库
   git remote remove origin
   # 重新添加
   git remote add origin https://github.com/你的用户名/jack-campus-social.git
   ```

2. **错误：authentication failed**
   - 需要配置 GitHub 认证
   - 使用 Personal Access Token（推荐）
   - 或使用 SSH 密钥

3. **错误：failed to push**
   ```bash
   # 先拉取远程更改
   git pull origin main --allow-unrelated-histories
   # 再推送
   git push -u origin main
   ```

---

## 七、GitHub 认证配置

### 方法 1：使用 Personal Access Token（推荐）

1. **生成 Token**：
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - 勾选 `repo` 权限
   - 生成并复制 Token

2. **使用 Token**：
   ```bash
   # 推送时，用户名输入你的 GitHub 用户名
   # 密码输入 Token（不是 GitHub 密码）
   git push -u origin main
   ```

### 方法 2：使用 GitHub CLI

```bash
# 安装 GitHub CLI
# Windows: winget install GitHub.cli

# 登录
gh auth login

# 之后就可以直接使用 git push
```

---

## 八、最佳实践

### 1. 提交频率

- ✅ **频繁提交**：每完成一个小功能就提交
- ❌ **不要**：等到所有功能完成才提交

### 2. 提交信息

- ✅ **清晰描述**：`完成用户注册功能`
- ❌ **避免**：`更新`、`修复bug`、`123`

### 3. 推送频率

- ✅ **每天结束前推送**：确保代码备份到云端
- ✅ **完成重要功能后推送**：防止丢失

### 4. 分支管理

- ✅ **主分支**：保持稳定，只合并完成的功能
- ✅ **功能分支**：开发新功能时创建分支

---

## 九、常见问题解答

### Q1: Git 和 GitHub 有什么区别？

**A**: 
- Git = 工具（本地）
- GitHub = 网站（云端）
- Git 可以单独使用
- GitHub 必须配合 Git 使用

### Q2: 为什么要推送到 GitHub？

**A**: 
- 备份代码（防止丢失）
- 多设备同步
- 多人协作
- 展示项目

### Q3: 如果忘记推送，文件会丢失吗？

**A**: 
- 如果已提交到本地 Git，**不会丢失**
- 可以从本地 Git 恢复
- 但推送到 GitHub 更安全（云端备份）

### Q4: 如何查看远程仓库地址？

```bash
git remote -v
```

### Q5: 如何修改远程仓库地址？

```bash
git remote set-url origin 新的URL
```

---

## 十、快速参考

### 最常用的 5 个指令

```bash
# 1. 查看状态
git status

# 2. 添加文件
git add .

# 3. 提交更改
git commit -m "描述"

# 4. 推送到 GitHub
git push

# 5. 拉取最新代码
git pull
```

---

## 总结

1. **Git** = 本地版本控制工具
2. **GitHub** = 云端代码托管平台
3. **工作流程**：编写代码 → `git add` → `git commit` → `git push`
4. **重要**：频繁提交和推送，防止代码丢失

现在你可以安全地管理代码了！🚀
