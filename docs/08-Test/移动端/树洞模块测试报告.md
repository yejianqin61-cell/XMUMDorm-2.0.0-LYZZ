# 移动端 App 树洞模块测试报告

**项目：** Dorm · XMUMDorm-2.0.0-LYZZ  
**测试对象：** `mobile/` 树洞模块（数据获取 + Tag 解析 + 瀑布流 + 分页 + 点赞）  
**测试日期：** 2026-06-01  
**测试框架：** Jest 30.4.2（Node 环境，CommonJS）

---

## 一、测试结果总览

```
Test Suites: 2 passed, 2 total
Tests:       39 passed, 39 total ✅ 100%
```

| 测试套件 | 用例数 | 通过 | 覆盖模块 |
|----------|--------|------|----------|
| `AuthContext.test.js` | 19 | 19 | 认证逻辑 |
| `TreeholeScreen.test.js` | 20 | 20 | 树洞模块 |
| **合计** | **39** | **39** | |

---

## 二、树洞模块测试用例清单

### 2.1 数据获取（4 用例）

| # | 用例 | 状态 |
|---|------|------|
| 1 | getPostList 正确拼接 page + pageSize 参数 | ✅ |
| 2 | 带 tagSlug 参数正确编码 | ✅ |
| 3 | 响应解析：list 数组 + hasMore 标志 + author 嵌套 | ✅ |
| 4 | 网络错误 → 降级返回空列表 | ✅ |

### 2.2 Tag 解析（4 用例）

| # | 用例 | 状态 |
|---|------|------|
| 5 | 中文环境用 name_zh、英文环境用 name_en | ✅ |
| 6 | 缺失英文名时回退到中文名 | ✅ |
| 7 | 去除 `# ` 前缀（`# 校园` → `校园`） | ✅ |
| 8 | 空对象不崩溃 → 返回空字符串 | ✅ |

### 2.3 瀑布流分列（3 用例）

| # | 用例 | 状态 |
|---|------|------|
| 9 | 偶数帖子均分（4条 → 左2右2） | ✅ |
| 10 | 奇数帖子左列多1（3条 → 左2右1） | ✅ |
| 11 | 空列表两列皆空 | ✅ |

### 2.4 分页与合并（3 用例）

| # | 用例 | 状态 |
|---|------|------|
| 12 | hasMore：10条=true, 9条=false, 0条=false | ✅ |
| 13 | mergePostsById 去重（重复 id 保留旧数据） | ✅ |
| 14 | 全重复不追加 | ✅ |

### 2.5 点赞逻辑（3 用例）

| # | 用例 | 状态 |
|---|------|------|
| 15 | 点赞 → liked=true, count+1；取消 → liked=false, count-1 | ✅ |
| 16 | like_count 下限为 0（不会出现负数） | ✅ |
| 17 | 点赞 API 返回值解析（liked + like_count） | ✅ |

### 2.6 时间格式化（3 用例）

| # | 用例 | 状态 |
|---|------|------|
| 18 | 几秒前 → "刚刚" | ✅ |
| 19 | 5 分钟前 → "5分钟前" | ✅ |
| 20 | null → 空字符串 | ✅ |

---

## 三、树洞模块当前完成度

| 功能 | 状态 | 说明 |
|------|------|------|
| 头部（XMUM Dorm + 搜索 + 语言 + 铃铛） | ✅ | 完整复刻 Web |
| Tag 工具栏（真实 API + 选中态 + 下划线） | ✅ | `getPostTagsList` + `getVisibleTags` |
| 双列瀑布流 | ✅ | `PostCardWaterfall` 组件 |
| 帖子详情（点赞 + 评论） | ✅ | `PostDetailModal` |
| 发帖（文字 + 图片上传） | ✅ | `NewPostModal` |
| 骨架屏 | ✅ | `TreeholeSkeletonCard` |
| 下拉刷新 + 无限滚动 + 返回顶部 | ✅ | `ScrollView` onScroll |
| FAB 发帖按钮 | ✅ | 右下角黑色圆形 + 按钮 |
| 帖子搜索 | ⚠️ 占位 | 搜索框 UI 就绪，未对接真实搜索页面 |
| Tag 管理面板（自定义可见标签） | ⚠️ 入口存在 | Plus 按钮已渲染，未弹出管理面板 |
| ReportButton（举报） | ❌ | 未集成 |

---

## 四、与认证模块测试汇总

| 指标 | 认证模块 | 树洞模块 | 合计 |
|------|----------|----------|------|
| 测试用例 | 19 | 20 | 39 |
| 通过率 | 100% | 100% | 100% |
| 覆盖层次 | API格式 + Token + 游客 + 角色 | 数据获取 + Tag + 瀑布流 + 分页 + 点赞 + 时间 |

---

**报告编写：** Claude Code (Claude Opus 4.8)  
**最后更新：** 2026-06-01
