# 万能墙（Confession Wall）模块测试报告 — M09

| 项 | 值 |
|----|----|
| 模块 | M09 万能墙（Confession Wall） |
| 设计文档 | [docs/04-Module/M09-万能墙/Module09-万能墙模块设计.md](../../04-Module/M09-万能墙/Module09-万能墙模块设计.md) |
| 测试范围 | Web 端（后端路由 + 前端纯逻辑） |
| 执行日期 | 2026-09-25 |
| 结论 | **全部通过** |

---

## 1. 执行汇总

```
Test Suites: 27 passed, 27 total
Tests:       375 passed, 375 total
```

其中本模块新增 4 个测试文件、**135 个用例**：

| 测试文件 | 用例数 | 覆盖对象 |
|----------|--------|----------|
| `__tests__/routes/confessions.test.js` | 69 | `routes/confessions.js` 全部 9 个接口 |
| `__tests__/frontend/confessionWindow.test.js` | 30 | `shared/utils/confessionWindow.js` |
| `__tests__/frontend/confessionKeyboard.test.js` | 27 | `shared/utils/confessionKeyboard.js` |
| `__tests__/shared/confessionTemplatesParity.test.js` | 9 | 前后端版式定义防漂移 |
| **合计** | **135** | |

回归影响：既有 23 个测试套件、240 个用例**零失败**。

### 构建验证

```
cd frontend && npx vite build
✓ built in 5.63s
dist/assets/ConfessionWall-62N4I9n0.js   19.49 kB │ gzip: 6.54 kB
```

新页面正常进入产物，无 import / 语法错误。

---

## 2. 按《测试铁律》矩阵的覆盖情况

《测试铁律》要求「所有 Route」必须覆盖六类场景。逐项对照：

| 要求场景 | 覆盖情况 | 代表用例 |
|----------|----------|----------|
| 正常请求 200 + 正确数据结构 | ✅ 9/9 接口 | `返回 200 + 正确的窗口数据结构`、`正常投稿返回 200 + 完整帖子对象` |
| 未登录 401 | ✅ 5 个需鉴权接口 | `未提供令牌返回 401`（用**真实** `authenticateToken` 中间件验证） |
| 参数缺失/无效 400 | ✅ | `正文为空返回 400`、`非法版式返回 400`、`非法 id 返回 400` |
| 资源不存在 404 | ✅ | `帖子不存在返回 404`、`评论不存在返回 404` |
| 无权限 403 | ✅ | `非作者且非管理员返回 403`、`他人评论返回 403`、`令牌无效返回 403` |
| 数据库错误 500 | ✅ 9/9 接口 | `数据库错误返回 500` |

---

## 3. 匿名性专项（本模块最重要的安全不变量）

匿名是万能墙的核心承诺，因此设了专门的 `describe('匿名性（安全不变量）')` 区块，
核心是 `assertNoIdentityLeak(res)`：**递归遍历整个响应 JSON**，一旦出现
`user_id` / `username` / `nickname` / `avatar` / `from_user_id` 任一键即测试失败。

关键设计：测试数据 `RAW_CONFESSION` / `RAW_COMMENT` **故意包含** `user_id`、`username`、
`nickname`、`avatar` 等身份列，用以证明路由是**主动逐字段投影**（`projectConfession` /
`projectComment`）来保证匿名，而不是依赖 SQL「碰巧没选这些列」——后者一旦有人改动
SELECT 就会立刻泄漏。

| 用例 | 验证内容 |
|------|----------|
| `GET /window 不下发任何身份字段` | 列表窗口响应干净 |
| `GET /window 使用带身份字段的原始行时，仍被投影掉` | 即使 DB 行含身份列也不泄漏；并断言响应文本不含 `真实昵称` / `leaker_should_never_appear` |
| `GET /:id 不下发任何身份字段（即使 DB 行含身份列）` | 详情响应干净 |
| `GET /:id/comments 不下发任何身份字段（含 replies）` | 一级评论与**其 replies** 都干净 |
| `POST /comments 回读行含身份列时也不下发` | 写入后回读同样投影 |
| `POST / 投稿响应不下发身份字段` | 投稿响应干净 |

`viewer_is_mine` / `viewer_is_author` 为**布尔值**，只回答「是不是你自己发的」，
不含身份标识，因此不违反匿名性——它们不在禁止键列表中，且其语义已被评论面板正确消费。

---

## 4. 路由测试明细

### 4.1 `GET /api/confessions/window`

| 用例 | 断言 |
|------|------|
| 返回 200 + 正确结构 | `items` / `total` / `has_older` / `has_newer` / `oldest_cursor` / `newest_cursor` |
| 无内容 | `items: []`、`total: 0`、`oldest_cursor: null` |
| `limit=999` | SQL 中钳制为 `LIMIT 10` |
| `limit=abc` | 回落为 `LIMIT 5` |
| `direction=older` | SQL 含 `id < ?` + `ORDER BY id DESC`；参数为 `[100]` |
| `direction=newer` | SQL 含 `id > ?` + `ORDER BY id ASC`，且结果被**应用层反转为「新→旧」** |
| 锚点已被删除 | 正常返回相邻内容，不报错 |
| DB 错误 | 500 |

### 4.2 `POST /api/confessions`

正常投稿；缺省版式回落 `bigtype`（断言 INSERT 参数为 `[9, 'bigtype', ...]`）；
非法版式 400；空/纯空格正文 400；缺失正文 400；
**逐版式长度上限边界**：大字卡 60 通过 / 61 拒绝，便签卡 301 拒绝，信笺卡 1000 通过 / 1001 拒绝；
`<script>` 与 `<img onerror>` 被清洗为纯文本入库；未提供令牌 401；令牌无效 403；
有效令牌可投稿且 `req.user.id` 正确传入（`query.mock.calls[0][1][0] === 9`）；DB 错误 500。

### 4.3 `GET / DELETE /api/confessions/:id`

详情正常返回；非法 id 400；不存在 404；**已删除/隐藏同样按 404 响应以避免泄露存在性**；
作者本人删除成功并写入审计日志（`action: 'CONFESSION_DELETE'`）；非作者非管理员 403；
不存在 404；未提供令牌 401；DB 错误 500。

### 4.4 点赞 `POST /:id/like`

首次点赞 `liked: true` + 最新计数；再次点赞为**取消** `liked: false`；
帖子不存在 404；非法 id 400；未提供令牌 401；DB 错误 500。

### 4.5 评论

| 用例 | 断言 |
|------|------|
| 一级评论 | 成功，`parent_id: null` |
| 回复一级评论 | 成功，`parent_id: 501` |
| **回复一条回复** | 400 `仅支持二级评论，不能回复回复` |
| `parent_id` 跨帖 | 400 `回复的评论不存在` |
| 空内容 / 超 500 字 | 400 |
| 含 HTML | 清洗后入库（`<b>加粗</b>文字` → `加粗文字`） |
| 删除本人评论 | 成功 + 审计日志 `CONFESSION_COMMENT_DELETE` |
| 删除他人评论 | 403 |

---

## 5. 前端纯逻辑测试明细

### 5.1 `confessionKeyboard`（27 例）

表驱动覆盖全部键位映射；**两条硬规则的逐项验证**：

- **焦点让位**：`INPUT` / `TEXTAREA` / `SELECT`（含小写）/ `contenteditable` 下，
  方向键、Home/End、空格、Enter 全部返回 `null`（不拦截），仅 `Escape` 例外。
- **修饰键**：`metaKey` / `ctrlKey` / `altKey` 任一按下即完全退出。
- **边界**：`key` 缺失、`ctx` 为 `null`、`defaultPrevented` 均安全返回而非抛错。

> 测试过程中发现并修复了一个真实缺陷：`ctx = {}` 的默认参数无法处理显式传入 `null`，
> 会抛 `TypeError: Cannot read properties of null`。已改为 `} = ctx || {};`。

### 5.2 `confessionWindow`（30 例）

| 分组 | 关键断言 |
|------|----------|
| `applyWindowPage` | 顺序、index 归 0、空/非法输入安全降级 |
| `appendOlder` / `prependNewer` | 追加/前插正确；**前插后 index 同步后移，仍指向同一篇**；重复 id 不重插 |
| `move` | 窗口内移动；到边界时正确返回 `needsLoad: 'older'/'newer'`；**确实到头时返回 `moved:false, needsLoad:null`**（触发回弹而非请求）；空窗口/未知方向不抛错 |
| `jump` | Home/End 正确；空窗口安全 |
| `trimWindow` | 超上限裁剪后**当前下标仍指向同一篇**；`entries` 不残留已淘汰 id；自定义 max 生效 |
| `shouldPrefetch` | 近尾→`older`、近头→`newer`、中部→`null`、无更多内容→`null` |
| `patchEntry` / `removeEntry` | 只改目标；移除前置条目时 index 前移以保持指向；移除最后一篇后窗口归空、index 归 0 |

### 5.3 `confessionTemplatesParity`（9 例）

守护 `constants/confessionTemplates.js`（后端 CJS）与
`shared/constants/confessionTemplates.js`（前端 ESM）两份刻意双胞胎的一致性：
key 集合与顺序、`maxLength`、双语标签与描述、默认版式、
`normalizeTemplateKey` / `getTemplate` / `getTemplateMaxLength` 行为。
**任何人只改一边，这里立刻失败。**

---

## 6. 设计文档 §10 边界条件清单的验证对照

| 边界条件 | 状态 | 依据 |
|----------|------|------|
| 墙为空 | ✅ | `无内容时返回空数组与 total 0` + 空态 UI |
| 锚点游标指向已删除的帖 | ✅ | `锚点游标指向已删除的帖时不报错` |
| 列表末尾继续翻下篇 | ✅ | `move` 的 `needsLoad: null` 用例 + `flashEdge` 回弹 |
| `limit` 999 / -1 / abc | ✅ | 钳制用例（abc 回落 5） |
| 正文只含空格换行 | ✅ | `正文为空返回 400` |
| 正文含 `<script>` | ✅ | 清洗用例 |
| 正文含敏感词 | ⚠️ 由中间件保证 | `sensitiveWordFilter` 在测试中被 mock（其自身已有单测） |
| 大字卡 61 字 | ✅ | `大字卡超过 60 字返回 400` |
| 便签卡含多换行 | ✅ | `NoteBody` 按行渲染为要点；含 20 行属渲染层，未做 DOM 测试 |
| 未登录点赞 | ✅ | 401 用例 |
| 重复点赞 | ✅ | 切换语义用例（主键冲突由 `INSERT IGNORE` + 主键兜底） |
| 回复一条回复 | ✅ | 400 用例 |
| 删除他人评论 | ✅ | 403 用例 |
| 删除自己的帖子 | ✅ | 成功 + `removeEntry` 滑窗逻辑用例 |
| 管理员后台删帖 | ⚠️ 人工 | 需在管理员后台手工验证（`CONTENT_MODULES.confession`） |
| 帖子被隐藏后直接访问 API | ✅ | 404 不回显存在性 |
| 重复举报 | ⚠️ 复用既有 | `routes/reports.js` 既有防重逻辑（其自身测试已覆盖） |
| 快速连按方向键 20 次 | ⚠️ 人工 | 节流与动效需真实浏览器验证 |
| 输入框内按方向键 | ✅ | 焦点让位表驱动用例 |
| 输入框内按 Esc | ✅ | 唯一放行用例 |
| `prefers-reduced-motion` | ⚠️ 人工 | CSS 媒体查询已写，需浏览器验证 |

---

## 7. 端到端接口验证（真实数据库）

除单元/集成测试外，另启动真实后端（`node server.js`）对**真实数据库**跑了一遍完整生命周期，
全部通过：

| 步骤 | 结果 |
|------|------|
| 1. 投稿（信笺卡） | 200，`author` 返回 `{"display_name":"匿名",...}`，`viewer_is_author: true` |
| 2. 窗口接口取到该篇 | 200，`window ids: [1]`，`total: 1` |
| 3. 单篇详情 | 200，正文含换行正确保留 |
| 4. 点赞 | 200，`{liked: true, like_count: 1}` |
| 5. 再次点赞（取消） | 200，`{liked: false, like_count: 0}` |
| 6. 评论 + 回复 | 均 200，`parent_id: 1` |
| 7. 回复一条回复 | **400** `仅支持二级评论，不能回复回复` |
| 8. 评论列表 | 200，一级 1 条 + `replies` 1 条，作者为「匿名」，`viewer_is_mine: true` |
| 9. 非法版式 | **400** `无效的版式` |
| 10. 大字卡 61 字 | **400** `正文超出该版式字数上限（60 字）` |
| 11. 未鉴权投稿 | **401** |
| 12. 匿名性全链路扫描 | 投稿 / 窗口 / 详情 / 评论 / 评论列表 **5 个响应全部干净** |
| 13. 删除投稿 | 200 `删除成功`，`total` 回到 0 |

验证产生的测试行已**彻底清除**（非逻辑删除）：`confessions` / `confession_comments` /
`confession_likes` 三张表均已确认为 **0 行**，生产库未留下任何测试数据。

> 顺带发现一个产品级行为，**已记录待决**：`DELETE /api/confessions/:id` 会逻辑删除帖子，
> 但不会级联软删除其下的评论（`confession_comments` 的外键指向帖子行，帖子是软删除，
> 所以评论仍在）。当前表现是：帖子从墙消失、评论数据残留在库中且不再可达。
> 若希望删帖时一并清理评论，需要新增逻辑（属后续小改动，不影响本期功能可用性）。

---

## 8. 未覆盖项与后续建议

1. **浏览器端手工验证（建议在真机/桌面浏览器执行一遍）**
   - 纵向翻页动效的连贯性、快速连按方向键的表现
   - `prefers-reduced-motion` 降级效果
   - 三款版式在窄屏 / 宽屏下的排版
   - 点赞乐观更新与失败回滚的视觉反馈
   - 管理员后台「万能墙帖子」页签的列表、详情与隐藏/恢复

2. **未做组件级 DOM 测试**
   根因：`frontend/` 自带 `package.json`（`"type": "module"`），根 Jest 的 Babel 配置
   不作用于其下的文件，因此无法直接渲染测试 `frontend/src/**` 的 JSX 组件。
   如需组件测试，需为 `frontend/` 单独配置 Jest + Testing Library（独立立项）。

3. **`frontend-app/`（Capacitor 端）未同步**
   本期范围为「仅 Web 端」（决策 Q6）。若后续要覆盖 App，需把
   `frontend/src/pages/ConfessionWall.*` 与 `components/confession/` 复制到 `frontend-app/`，
   两者此前是各自维护的（见 `__tests__/frontend/treeholeRecommendationCopy.test.js` 同时检查两个目录）。

4. **移动端 TabBar 未新增 Tab**
   `/confession` 在移动端通过路由可访问，但 `getTabIndex` 会使其归到第 0 个 Tab（广场）而高亮略偏。
   这是决策 Q6「不改 Layout 四格结构以免波及树洞」的已知取舍。

---

## 9. 生产库迁移状态

⚠️ 064 / 065 的建表已在生产库生效（三张空表），经过一次误操作事故，详见
[设计文档 §16 事故记录](../../04-Module/M09-万能墙/Module09-万能墙模块设计.md)。
经核实：结构、索引、外键均与设计一致，**0 行数据，业务数据未受影响**。
下次部署无需再执行迁移（`CREATE TABLE IF NOT EXISTS` 亦会安全跳过）。
