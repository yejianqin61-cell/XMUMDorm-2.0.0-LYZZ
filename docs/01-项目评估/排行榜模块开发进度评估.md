# 排行榜模块开发进度评估（紧急自查）

> 自查结论：**排行榜前端 UI 与路由目前均为 0，未开发。**

---

## 一、自查结果汇总

| 项 | 状态 | 说明 |
|----|------|------|
| **React 路由** | ❌ 未做 | `App.jsx` 中无 `/rank`、`/rankings`、`/eat/rankings` 等任何排行榜路由 |
| **排行榜页面** | ❌ 未做 | `frontend/src/pages/` 下无 Rankings / RankList / Leaderboard 等页面组件 |
| **排行榜相关组件** | ❌ 未做 | 无榜单卡片、榜单列表、Tab 切换（最夯单品/门庭若市/最夯商家/爆款新品/点评达人）等 UI |
| **底部 Tab 入口** | ❌ 未做 | `TabBar.jsx` 仅 4 个 Tab：树洞、食堂、关于我们、我的；无「排行榜」Tab |
| **食堂内入口** | ❌ 未做 | `CanteenArea.jsx` 仅展示分区卡片，无「排行榜」入口链接/按钮 |
| **前端 API 封装** | ❌ 未做 | `api/canteen.js` 未封装 rankings 相关接口（hot-products、busy-shops、top-shops、new-hit-products、active-users） |
| **后端 API** | ✅ 已有 | `routes/canteen.js` 已实现 5 个排行榜接口（见下） |

---

## 二、后端已有接口（供前端对接）

| 接口 | 说明 |
|------|------|
| `GET /api/canteen/rankings/hot-products` | 最夯单品榜 Top 5 |
| `GET /api/canteen/rankings/busy-shops` | 门庭若市商家榜 Top 5 |
| `GET /api/canteen/rankings/top-shops` | 最夯商家榜 Top 5 |
| `GET /api/canteen/rankings/new-hit-products` | 爆款新品 Top 3 |
| `GET /api/canteen/rankings/active-users` | 点评达人榜 Top 5 |

业务规则与周期（每周一东八区重置等）见 `md笔记/排行榜系统行动计划.md`。

---

## 三、当前缺失清单（需补齐）

1. **路由**  
   - 在 `App.jsx` 中新增排行榜路由（建议路径：`/eat/rankings` 或 `/rankings`，与产品/设计约定即可）。

2. **页面**  
   - 新增排行榜页面组件（如 `Rankings.jsx` 或 `RankingsPage.jsx`），负责：
     - 展示 5 个榜单（可 Tab 或分段展示）；
     - 调用上述 5 个 API 获取数据；
     - loading / 空态 / 错误态。

3. **入口**  
   - **二选一或都要**：  
     - 在食堂首页 `CanteenArea` 增加「排行榜」入口（按钮/卡片），跳转到排行榜页；  
     - 或在底部 `TabBar` 增加「排行榜」Tab（若采用，需与现有 4 个 Tab 的布局约定，如改为 5 个或收起到「更多」）。

4. **Layout**  
   - 在 `Layout.jsx` 中为排行榜路径配置标题（如「排行榜 Rankings」）与返回（若为子路径如 `/eat/rankings`）。

5. **前端 API 层**  
   - 在 `api/canteen.js`（或单独 `api/rankings.js`）中封装上述 5 个 GET 接口，供排行榜页面调用。

---

## 四、结论与建议

- **进度**：排行榜模块前端目前为 **0%**（路由、页面、组件、入口、API 封装均未做）；后端为 **100%**（5 个接口已实现）。
- **建议**：  
  1. 先定路由与入口（是否独立页 `/rankings` 还是食堂下 `/eat/rankings`，是否加 Tab）。  
  2. 再补：路由 + 排行榜页面 + 食堂/或 Tab 入口 + Layout 标题 + 前端 rankings API 封装。  
  3. 页面内可先做 5 个榜单的列表/卡片 UI，再接真实接口并做 loading/错误处理。

如需，我可以按你定的路由与入口方案，直接给出 `App.jsx`、`Layout.jsx`、`TabBar` 或 `CanteenArea` 的修改片段，以及 `Rankings.jsx` 与 `api/canteen.js` 的排行榜封装与调用示例。
